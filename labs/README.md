#Labs

```bash
cd labs
# Create a network on docker
sudo docker network create labs-nv

sudo docker network inspect labs-nv

# get Subnet, my subnet is "Subnet": "172.19.0.0/16",

chmod -x ./kind/kind-with-registry.sh 

```
